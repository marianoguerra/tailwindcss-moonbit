target/
_build/
.mooncakes/
node_modules/
tools/oracle/node_modules/
tools/diff/.work/

# Built MoonBit->JS module, copied next to the wrapper for local use / npm publish
ffi/js/ffi.js

# Benchmark run outputs (regenerated by benchmarks/run.mjs); fixtures ARE committed
benchmarks/results/

# Compiler modules the showcase site loads, built by `just build-site` and by
# the pages workflow. Everything else under web/site/assets IS committed.
web/site/assets/twffi.wasm
web/site/assets/twffi.js

# Screenshots from browser-driven checks
.playwright-mcp/

# Symlink to README.mbt.md that moon recreates on every build/test. Anchored:
# unanchored, this pattern also swallows every nested README (web/, ffi/, ...).
/README.md

